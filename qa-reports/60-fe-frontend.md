# گزارش بازبینی فرانت‌اند فاز ۲ — Task 60-fe

- نقش: متخصص فرانت‌اند · دامنه: اثر فیکس‌های فاز ۲ (BUG-01…05) روی UI/UX فروشگاه
- روش: بازخوانی کد (product/*، product/[slug]/*، shop/*، checkout/*، admin orders/*) + پروب زندهٔ `curl` روی dev server (سبز: `/api/health` 200، db connected، products=13)
- محصولات بررسی‌شدهٔ زنده: `/product/test-inactive-all-variants` (HTTP 200، ۱۵۳KB) و `/shop`
- ممنوعات رعایت شد: هیچ فایل سورسی ویرایش نشد، بدون commit/push، بدون `/admin/settings`، بدون ری‌استارت سرور

---

## ۱) سناریوی محوری: محصول با همهٔ واریانت‌های غیرفعال (stock=0, price=0, sku="", colors/sizes=[])

### ✅ درست کار می‌کند — شواهد
| مورد | شواهد |
|---|---|
| CTA صفحهٔ محصول «ناموجود» + غیرفعال | `src/app/product/[slug]/product-info.tsx:188-192` (`disabled={product.stock === 0}`، برچسب شرطی «ناموجود») — زنده: دقیقاً ۱ بار «ناموجود» در HTML |
| JSON-LD صادق | زنده: `availability: https://schema.org/OutOfStock` (دو بلاک RSC هم همان) — `page.tsx:85-98` |
| گارد «تنها X عدد در انبار» | `product-card.tsx:146` و `product-info.tsx:210` شرط `stock > 0` — زنده: صفر مورد «تنها … عدد» در /shop و PDP محصول تستی |
| قالب‌بندی فارسی قیمت | `src/lib/format.ts:5-16` (`Intl.NumberFormat("fa-IR")`) — زنده: «۱٬۴۹۰٬۰۰۰ تومان» و «۷۴۵٬۰۰۰ تومان» روی کارت‌های سالم |
| مپر BUG-05 | `src/lib/repositories/mappers.ts:173-196` — `activeVariants` فقط فعال‌ها؛ stock=0، price=0، sku=""، colors/sizes=[] — با تست‌های +۳ mapper هم‌خوان |

### 🟡 F-1 — نمایش «۰ تومان» برای محصول ناموجود (متوسط)
- صفحهٔ محصول: بلاک قیمت بزرگ می‌گوید **«۰ تومان»** در حالی که دکمه زیرش «ناموجود» است — عدد صفر برای مشتری معنا ندارد و «وعدهٔ قیمت کاذب» است (روح ADR 011 که BUG-05 روی آن کار کرد، فقط نیمهٔ موجودی‌اش بسته شد، نیمهٔ قیمت باز است).
  - کد: `product-info.tsx:80` → `<span class="text-2xl font-bold">۰ تومان</span>` (تأیید زنده)
- کارت محصول در /shop هم «۰ تومان» نشان می‌دهد: `product-card.tsx:121-123` (تأیید زنده: `font-semibold">۰ تومان` در HTML فروشگاه)
- پیشنهاد: وقتی `price === 0` قیمت نمایش داده نشود (یا «—») و کارت/PDP به برچسب «ناموجود» بسنده کند.

### 🟡 F-2 — دکمه‌های افزودن برای stock=0 گارد ندارند → خط صفرتایی در سبد (متوسط؛ همان خانوادهٔ BUG-07 فاز ۳)
- «افزودن سریع به سبد» کارت (`card-actions.tsx:41-62`) و «افزودن به سبد» علاقه‌مندی‌ها (`wishlist-view.tsx:86-98`) هیچ چک موجودی ندارند و غیرفعال نمی‌شوند.
- زنجیره: `addToCart` → `quantity: Math.min(1, 0) = 0` (`cart-store.ts:102`) → خط صفرتایی (ghost) + باز شدن drawer سبد (`cart-store.ts:114`) → در چک‌اوت سرور تمیز رد می‌کند: «یکی از کالاهای سبد قابل خرید نیست.» (`checkout/actions.ts:98-100`)
- یعنی UI وعده می‌دهد و سرور رد می‌کند — پول در خطر نیست ولی تجربه ناسازگار است. (پیش از این در tasks.md فاز ۳ به‌عنوان BUG-07 ثبت شده؛ این تأیید UI آن است.)
- در تضاد: CTA اصلی PDP (`product-info.tsx:188`) درست غیرفعال می‌شود — رفتار دو جزیره ناهمگون است.

### 🔵 F-3 — سکشن‌های «رنگ:» و «سایز:» خالی در PDP (پایین)
- با `colors=[]`/`sizes=[]` تیترها با مقدار خالی رندر می‌شوند و لینک «راهنمای انتخاب سایز» هم می‌ماند — کد: `product-info.tsx:90-156`؛ زنده: هر سه عبارت «رنگ:»، «سایز:»، «راهنمای انتخاب سایز» هر کدام ۱ بار با ظرف خالی. پیشنهاد: رندر شرطی این سکشن‌ها وقتی خالی‌اند.

### 🔵 F-4 — محصول تست QA عمومی و قابل‌دیدن در /shop است (پایین)
- توضیح کوتاه کارت می‌گوید «محصول تست QA فاز ۲ (BUG-05) — نباید به مشتری نمایش فریبنده بدهد» ولی چون status=ACTIVE است در گرید فروشگاه می‌آید (تأیید زنده: ۶ ارجاع به slug در HTML /shop). تصمیم محصولی: یا DRAFT شود، یا فهرست‌کردن محصول ACTIVE بدون هیچ واریانت فعال از product-service فیلتر شود (پیشنهاد ثبت در فاز ۳، کنار BUG-07).

---

## ۲) پیام‌های جدید کوپن مهمان (BUG-02)

### ✅ جریان UI سالم است
- منبع پیام: «ظرفیت این کد برای خرید مهمان پر شده — با ورود به حساب کاربری دوباره امتحان کنید.» در هر دو مسیر خواندنی و تراکنشی: `src/core/commerce/coupon-service.ts:95` و `:173`.
- اکشن: `validateCouponAction` خطای دامنه را به `{ok:false, message}` فارسی تبدیل می‌کند (`checkout/actions.ts:128-132`)؛ مسیر ثبت نهایی هم (`placeOrderAction`, `actions.ts:192-198`).
- UI: `checkout-client.tsx:135-141` → `toast.error(res.message)` — پیام فارسی کامل به کاربر مهمان می‌رسد (toast). rate-limit کوپن هم پیام فارسی دارد (`actions.ts:121`).

### 🔵 F-5 — پیام کوپن فقط toast است و CTA ورود ندارد (پایین)
- خطا فقط ۳ ثانیه toast می‌ماند؛ زیر فیلد کد تخفیف خطای inline رندر نمی‌شود (الگوی `Field` + `role="alert"` در همین فایل هست ولی به کوپن وصل نیست) — `checkout-client.tsx:424-454`.
- پیام می‌گوید «با ورود به حساب …» ولی صفحهٔ چک‌اوت مهمان هیچ لینک/دکمهٔ ورود نزدیک فرم ندارد.
- جای toast همچنان پیش‌فرض sonner (بدون `position` در `src/components/ui/sonner.tsx`) → UX-07 شناخته‌شده (پوشاندن دکمه‌ها) پابرجاست.

---

## ۳) پیام‌های خطای گذار ادمین (BUG-04)

### ✅ تبدیل تمیز به ActionResult — بدون 500 خام (بازبینی کد)
- منبع: `src/domain/state-machines/index.ts:188-212` — 409 «گذار وضعیت سفارش از X به Y مجاز نیست» / 403 «گذار X → Y توسط actor مجاز نیست (مجری‌های مجاز: …)».
- `order-service` از همان جدول تغذیه می‌شود و `cancelOrder` بازیگر الزامی دارد (`order-service.ts:60`)؛ اکشن ادمین صریحاً `actor: "admin"` می‌فرستد (`src/app/admin/(panel)/orders/actions.ts:43-48`).
- wrapper `withAdminAction`: DomainError → `{ok:false, error:{code,message}}`، Zod → «داده‌های فرم معتبر نیست.» + fieldErrors، سایر → «خطای غیرمنتظره در سرور رخ داد.» (`src/lib/admin/action-helpers.ts:61-90`) — مسیری که 500 خام به UI بدهد وجود ندارد.
- UI: `orders-manager.tsx:188-198` → `toast.error(res.error?.message)` — پیام فارسی تمیز. دکمه‌ها هم با گاردهای سمت کلاینت (`canShip/canDeliver/canCancel/canReturn`, خط ۲۰۰-۲۰۳) از گذار نامعتبر جلوگیری می‌کنند؛ پیام‌های 403/409 فقط در رقابت ظاهر می‌شوند.
- 🔵 F-6 (پایین): پیام‌ها شناسه‌های انگلیسی وضعیت را داخل متن فارسی دارند («از PENDING به MARS») — `index.ts:197,205`. خوانا ولی دو-زبانه؛ در فاز بهداشت می‌شود statusFa نگاشت کرد. پروب زندهٔ اکشن ادمین نیازمند نشست بود و انجام نشد (ممنوعیت‌ها + عدم دستکاری داده)؛ استناد همین‌جا کد است.

---

## ۴) لینت ظاهری / RTL / حالت‌های خالی

| مورد | وضعیت | شواهد |
|---|---|---|
| RTL ریشه | ✅ دست‌نخورده | `src/app/layout.tsx:69` `lang="fa" dir="rtl"` (و :96)؛ `dir="ltr"` فقط جزیره‌ای و بجا (تلفن/کدپستی/کوپن `checkout-client.tsx:305,336,432`، گالری `gallery.tsx:24`) |
| حالت خالی گرید فروشگاه | ✅ | `product-grid.tsx:15-24` «محصولی پیدا نشد» |
| حالت خالی سفارش‌های ادمین | ✅ | `orders-manager.tsx:164-170` «سفارشی با این فیلتر یافت نشد.» |
| حالت خالی سفارش‌های حساب | ✅ | `account-panels.tsx:51` «هنوز سفارشی ثبت نکرده‌اید» |
| حالت خالی علاقه‌مندی‌ها | ✅ | `wishlist-view.tsx:37-53` کارت خالی با CTA («لیست علاقه‌مندی‌ها خالی است») — زنده: `/wishlist` 200 |
| حالت خالی نظرات | ✅ | `product-tabs.tsx:80-83` «هنوز نظری برای این محصول ثبت نشده است.» |
| اسکلتون‌های بارگذاری | ⚪ نکته | هیچ `loading.tsx` در `src/app/**` نیست (Glob خالی)؛ shop/PDP سرور-رندرند و Suspense فروشگاه fallback خالی دارد (`shop-view.tsx:67,84`) — رگرسیون فاز ۲ نیست، فقط ثبت مشاهده |
| رندر Rating برای محصول بی‌نظر | ✅ قابل‌قبول | `rating.tsx:19-40` — ستاره‌های خالی + «۰» با aria-label صادق («امتیاز ۰ از ۵»، تأیید زنده)؛ جعل نمی‌کند |

## ۵) اکسس‌بیلیتی — تأیید موارد شناخته‌شده (طبق دستور، فقط تأیید)
- **UX-12 (alt گالری):** وضعیت بهتر از ثبت‌شده است — تصاویر اصلی alt توصیفی دارند: ``alt={`${name} — تصویر ${i+1} از ${n}`}`` (`gallery.tsx:41`)؛ بندانگشتی‌ها `alt=""` تزئینی‌اند ولی دکمهٔ والد `aria-label="نمایش تصویر n"` دارد (`gallery.tsx:74`) — الگوی قابل‌قبول. ✅
- **UX-05 (کنتراست terracotta):** پابرجا و دست‌نخورده — `globals.css:41` هنوز `--color-terracotta: #c88f72` (با متن سفید < ۴.۵:۱)؛ CTAهای terracotta («افزودن به سبد» `product-info.tsx:185`، «پرداخت و ثبت سفارش» `checkout-client.tsx:466`) تحت‌تأثیرند. فاز ۲ چیزی را خراب‌تر نکرده؛ رفع در فاز ۴. ✅ (تأییدِ ثبوت)

---

## جمع‌بندی
فیکس‌های فاز ۲ در سطح UI «دروغ نمی‌گویند»: ناموجود صادق، JSON-LD صادق، خطاهای کوپن/گذار فارسیِ تمیز و بدون 500. باقی‌مانده‌ها نمایشی‌اند — مهم‌ترین‌ها: «۰ تومان» (F-1) و دکمه‌های افزودن بدون گارد موجودی (F-2، پیوند با BUG-07 فاز ۳). هیچ یافتهٔ بلوکه‌کننده‌ای نیست؛ پیشنهاد می‌شود F-1 و گارد F-2 به فهرست فاز ۳/۴ اضافه شوند.
