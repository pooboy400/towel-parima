# گزارش ممیزی سوئیت تست فاز ۲ — Task ID: 60-ts

**نقش:** مهندس تست (ممیزی سخت‌گیرانه، بدون تغییر کد)
**دامنه:** ۱۷ تست جدید فاز ۲ · دو دور اجرای integration · بهداشت DB · typecheck/lint · QA-HYG-01
**تاریخ:** 2026-09-26 (اسنپ‌شات 12:32–12:50 UTC) · شواهد: `qa-reports/tmp-60/int-run*.raw`، `cases-run*.txt`، `hygiene` خروجی‌ها
**محدودیت رعایت‌شده:** هیچ فایلی در `src/` یا `tests/` ویرایش نشد؛ commit/push و ری‌استارت سرور انجام نشد.

> ⚠️ نکتهٔ محیطی مهم: حین ممیزی من، دو ایجنت موازی دیگر فاز ۲ (60-fe و 60-sec؛ گزارش‌های `60-fe-frontend.md` و `60-sec-security.md`) روی **همین DB** و همین پوشهٔ `qa-reports/tmp-60/` فعال بودند (اسکرین‌شات‌های u1/u2/u3، اسکریپت‌های attack*.ts، flood-and-cancel). اثرات آن‌ها هرجا مرتبط بوده جدا و صادقانه برچسب خورده است.

---

## ۱. اجراها و پایداری (سؤال ۱ — flaky؟)

| اجرا | دامنه | نتیجه | خروجی |
|---|---|---|---|
| دور ۱ | `bun test tests/integration` | **65/65** · 0 fail · 554 expect · ۹ فایل · 1.92s | `int-run1.raw` |
| دور ۲ | همان | **65/65** · 0 fail · 554 expect · 4.23s | `int-run2.raw` |
| دور ۳ | فقط ۴ فایل فاز ۲ (coupon-concurrency + payment-fail-race + state-machines + mappers) | **45/45** · 0 fail | `p2-run3.raw` |
| دور ۴ | همان | **45/45** · 0 fail | `p2-run4.raw` |
| unit کامل | `bun test tests/unit` | **180/180** · 15 فایل · 546 expect · 301ms | `unit-run1.raw` |
| مجموع | unit + integration | **245/245** · 24 فایل · 1100 expect — ادعای Task 60 دقیقاً تأیید | `all-run.raw` |

**مقایسهٔ بایت‌به‌بایت دور ۱ و ۲ (integration کامل):**

- diff خام: ۲۳۲ خط تفاوت — **همه** در این دسته‌ها: مدت‌زمان `[xx.xxms]`، timestamp لاگ‌های mock-sms (`"at"`)، کدهای OTP تصادفی، شماره موبایل تصادفی فیکسچرها، CUID فیکسچرها داخل لاگ‌های `revalidateTag`، و زمان کل اجرا.
- **توالی خطوط (pass/fail) بعد از حذف مدت‌زمان‌ها: بایت‌به‌بایت یکسان — ۶۵ مورد، هیچ جابه‌جایی ترتیب (`cases-run1.txt` ≡ `cases-run2.txt`).**
- تنها تفاوت ساختاری: ۴ خط لاگ `revalidateTag` (async، fire-and-forget) در دور ۱ زودتر چاپ شدند — جابه‌جایی بافر لاگ، نه تفاوت رفتار. (این لاگ‌ها خودشان نشانهٔ یک cleanup زودهنگام در teardown تست‌ها هستند که قبلاً هم شناخته شده و بی‌ضرر است.)
- دورهای ۳ و ۴ (پس از فشار رقابتی ایجنت موازی روی همان DB): توالی ۴۵ مورد یکسان.
- **رأی flaky: خیر.** هیچ وابستگی به `Date.now()` در مسیرِ نتیجه (فقط در نام فیکسچر)؛ قفل‌های DB قطعی‌اند؛ هیچ نشانه‌ای از تداخل پول اتصال (max_connections=40، ۱۰ tx موازی مسئله‌ای ندارد).
- نکتهٔ مثبت: هر دو دور من دقیقاً هم‌زمان با ترافیک زندهٔ UI ایجنت موازی اجرا شد و همچنان ۲×۶۵ سبز بود — مقاومت تست‌های رقابتی به نویز محیط قابل قبول است.

---

## ۲. نگاشت معیار پذیرش tasks.md → تست (سؤال ۲)

| معیار (عین عبارت tasks.md / دستور ممیزی) | تست نگاشت‌شده | رأی | توضیح |
|---|---|---|---|
| **BUG-01** — «۱۰ موازی perUser=1 → دقیقاً ۱ Redemption» | `coupon-concurrency.test.ts` > «۱۰ مصرف موازی یک کاربر با سقف perUser=۱» — سه assert: okCount=1، `CouponRedemption.count=1`، `usedCount=1` | ✅ | پوشش کامل و سه‌لایه؛ اگر قفل FOR UPDATE حذف شود، count-then-create در ReadCommitted دود می‌شود و تست قرمز می‌گردد (تحلیل ذهنی mutation — اجرای جهش ممنوع بود) |
| **BUG-02** — «مهمان ۳ شماره → ۱ مصرف» | `coupon-concurrency.test.ts` > «مهمان با ۳ شمارهٔ مختلف…» — okCount=1 + `redemption(userId:null)=1` | ✅ | پیش از فیکس (شرط `&& userId`) هر ۳ موفق می‌شد → تست قرمز. ارزش محافظتی بالا |
| **BUG-03** — «لغو همزمان×callback → FAILED+Outbox؛ callback دوم بدون خطا» | `payment-fail-race.test.ts` تست ۱ + تست ۲ | ✅ | تست ۱: Payment=FAILED + `metadata.failReason` + Order=CANCELLED + **دقیقاً ۱** رخداد `PaymentFailed` در Outbox + رزرو RELEASED + `reserved=0`. تست ۲: callback دوم `resolves.toBeUndefined()` + صفر رخداد تکراری. هر دو نیمِ معیار پوشش داده شد |
| **BUG-04** — «assertOrderTransition(PENDING,CANCELLED,admin) مجاز» | `state-machines.test.ts` > ۵ تست BUG-04 | ✅ | هستهٔ معیار: `rule.actor==="admin"` + `adminPermission==="orders.update"`؛ بعلاوهٔ 403/409 تفکیک‌شده، حفظ مسیر customer، و actor=system |
| **BUG-05** — «دو واریانت غیرفعال → stock نمایشی 0» | `mappers.test.ts` > «دو واریانت غیرفعال → stock=0…» | ✅ | stock=0 + price=0 + صفر رنگ/سایز + میخِ فعال/غیرفعال (stock=3) + واریانت deletedAt=شمرده‌نشده. *(جزء دوم معیار tasks.md «صفحهٔ محصول حالت ناموجود» فقط-زنده است — الگوی پذیرفته‌شدهٔ پروژه برای UI؛ دادهٔ ورودی صفحه یعنی stock=0/colors=[] تست شده)* |

**جمع: ۵/۵ معیار ✅** (یک جزء UI در BUG-05 فقط-زنده).

### مرور کیفیت تک‌تک ۱۷ تست

**coupon-concurrency (۶):**
1. ۱۰ موازی perUser=1 — **۹/۱۰** — قوی؛ triple-assert.
2. مصرف دوم سریالی → COUPON_INVALID — **۷/۱۰** — پیش از فیکس هم سبز بود (count-then-create سریالی درست است)؛ ارزشش گارد رگرسیونی روی خودِ قاعدهٔ perUser است نه روی قفل.
3. مهمان ۳ شماره — **۹/۱۰** — هستهٔ BUG-02.
4. پیش‌نمایش مهمان با پیام شفاف — **۸/۱۰** — مسیر `evaluateCoupon` مهمان را سیم‌کشی می‌کند؛ assert پیام (`toContain("مهمان")`) کمی شکننده اما بامعنا (پیام ADR را قفل می‌کند).
5. جداسازی سبد مهمان/کاربر — **۸/۱۰** — تصمیم ADR را مستند و قابل‌شکست می‌کند (counts=2).
6. رگرسیون usageLimit ۱۰ موازی/۳ — **۸/۱۰** — نگهبان لایهٔ دوم (UPDATE شرطی).

**payment-fail-race (۳):**
7. رقابت لغو×callback — **۹/۱۰** — کامل‌ترین تست فاز ۲؛ ۶ assert همگرا؛ پذیرش منطقیِ تنها CONFLICT برای cancelOrder.
8. idempotency callback دوم — **۸/۱۰** — قرارداد بدون‌throw + بدون رخداد تکراری.
9. رقابت confirm×fail — **۶/۱۰** — if/else دوطرفه (هر خروجی می‌گذرد — ذاتی تست همگرایی، اما ضعیف‌تر)؛ تأیید واقعی با `updateMany` دستی شبیه‌سازی شده نه خودِ `confirmPayment`؛ **assert مردهٔ `expect(orderId).toBeTruthy()` (همیشه true — نویز)**.

**state-machines (+۵ BUG-04):** همه بامعنا — admin/customer/system بازیگر، تفکیک 403 از 409 — **میانگین ۸/۱۰**. (تست ۴۰۹ `SHIPPED→CANCELLED admin` با تست قدیمی «گذار رو به عقب» هم‌پوشانی جزئی دارد ولی با بازیگرِ مشخص ارزش اضافه دارد.)

**mappers (+۳ BUG-05):** سه تست با اعداد دقیق و حالت‌های مرزی (deletedAt، میخ فعال+غیرفعال) — **میانگین ۸.۵/۱۰**.

**میانگین کلی کیفی: ≈ ۷.۹/۱۰** — هیچ تست «تعدادسازی» (assert بی‌اثر) جز همان یک `expect(orderId).toBeTruthy()` دیده نشد.

---

## ۳. شکاف‌ها (سؤال ۳)

به ترتیب اهمیت:

1. **بازخوانی بعد از `FOR UPDATE` — غایب.** خطوط 127–133 `coupon-service.ts` (رد کوپن غیرفعال/حذف‌شده داخل پنجرهٔ قفل) هیچ تستی ندارد؛ حذفِ آن بلوک هیچ تستی را قرمز نمی‌کند. تست پیشنهادی: کوپن فعال → بین قفل و بازخوانی غیرفعال شود (شبیه‌سازی با tx دو-فازی یا فیکسچر hook) → رد COUPON_INVALID.
2. **`evaluateCoupon` مسیر `usageLimit` — غایب (تأیید شد، و ظریف‌تر از حدس اولیه):** `placeOrder` فقط `consumeCouponInTx` را صدا می‌زند؛ تست «usageLimit پر شده» در `commerce-core.test.ts:301` مسیر **مصرف** را می‌پوشاند نه مسیر **پیش‌نمایش** (`coupon-service.ts:82-84`، تنها caller واقعی: `checkout/actions.ts:126`). حذف آن گارد، هیچ تستی را قرمز نمی‌کند. (مسیر perUser مهمانِ همین تابع تست ۴ را دارد؛ مسیر perUser **کاربر** هم ندارد — جزئی.)
3. **تعامل BUG-03 با `refundRacedPayment` — غایب.** مسیر `confirmPayment` وقتی می‌بازد و سفارش لغو شده، استرداد خودکار (`payment-service.ts:225,239`) را شروع می‌کند؛ هیچ تستی این تعامل را نمی‌پوشاند — منطق پول واقعی و بدون نگهبان.
4. **حد بالای perUser>1 مهمان — غایب.** فقط perUser=1 تست شده؛ مرزِ perUser=2 (دو مصرف مهمان موفق، سومی رد) رفتار «سبد گمنام مشترک» را دقیق‌تر قفل می‌کرد.
5. (یافتهٔ خودم) **لغو ادمین از مسیر سرویس — فقط-unit.** `cancelOrder(actor:"admin")` در integration صدا زده نشده (تست race فقط actor:"customer")؛ اتصال سرویس↔جدول برای بازیگر ادمین بدون نگهبان integration است.
6. (جزئی) `evaluateCoupon` منقضی/شروع‌نشده و `minSubtotal` — در تست‌های فازهای قبل پوشش صریح دیدم/ندیدم ولی خارج دامنهٔ ممیزی فاز ۲ بود؛ برای فاز ۳ یک فایل unit کوچک coupon-evaluate کفایت می‌کند.

---

## ۴. بهداشت DB (سؤال ۴) — **❌ یک آلودگی سیستماتیک**

اسنپ‌شات اول (بعد از دو دور من، 12:4x) و دوم (12:49:51، پس از ۳ ران دیگرِ ایجنت‌های موازی):

| شمارش | انتظار | واقعی | رأی |
|---|---|---|---|
| `Coupon` با `code LIKE 'CC-%'` | 0 | **۵۰** (۳۵ در اسنپ‌شات اول) | ❌ **نشت** |
| Redemptionهای کوپن‌های CC-% | 0 | 0 | ✅ |
| سفارش‌های مهمان تستی (کد CC*/PF*، تلفن‌های 0914/0915/0916/0917/0918، نام آدرس تست) | 0 | 0 | ✅ |
| محصول `pfr-%` / واریانت `PFR%` | 0 | 0 | ✅ |
| پرداخت `auth-pfr-%` | 0 | 0 | ✅ |
| کاربران 0913%/0916% | 0 | 0 | ✅ |
| Outbox `PaymentFailed` ۳۰ دقیقه اخیر | 0 | 0 | ✅ |
| رزرو ACTIVE روی سفارش‌های تستی | 0 | 0 | ✅ |

**ریشهٔ نشت (تحلیل کد):** در `coupon-concurrency.test.ts` کد کوپن هنگام ساخت با `.toUpperCase()` ذخیره می‌شود (`CC-<ts>-<HEX>`) ولی afterAll با پیشوند **حروف کوچک** پاک می‌کند: `code: { startsWith: \`${RUN}-\` }` که `RUN = cc-<ts>` است — LIKE در Postgres حساس به بزرگی/کوچکی است ⇒ حذف با پیشوند **صفر ردیف**؛ تنها کوپنِ آخر (حذف با `id`) پاک می‌شود ⇒ **دقیقاً ۵ کوپن در هر ران نشت می‌کند**. ریاضی شواهد: ۳۵ = ۷ ران × ۵؛ سپس +۱۵ = ۳ ران × ۵ در ۱۲:۴۷–۱۲:۴۹ توسط ایجنت‌های موازی → ۵۰. نشت زنده و در جریان است. (فایل pfr از نظر بزرگی/کوچکی سازگار است — فقط فایل کوپن همین باگ را دارد.)
**اصلاح پیشنهادی یک‌خطی (اعمال نکردم — خارج از صلاحیت):** پاکسازی با `code: { startsWith: \`CC-${RUN.replace("cc-","")}-\` }` یا `mode: "insensitive"`؛ و پاکسازی دستی دادهٔ موجود: `DELETE FROM "Coupon" WHERE "code" LIKE 'CC-%'` (۱۵۰+ ردیف تا پایان باتری). ریسک جانبی: کوپن‌های نشتی `isActive=true` و الگوی کدشان قابل حدس‌زدن است — در dev بی‌ضرر ولی در لیست ادمین زباله می‌سازد و اگر روزی DB واقعی شود قابل‌سوءاستفاده است.

**دو ریسک الگویی (امروز بی‌ضرر، فردا خطرناک):**
- `db.user.deleteMany({ phone: { startsWith: "0913" } })` و `"0916"` — پیشوندهای موبایل **واقعی ایران** هستند؛ اگر روزی کاربر واقعی با این پیشوند ثبت‌نام کند، afterAll او را حذف می‌کند. حذف با `id` جمع‌آوری‌شده امن‌تر است.
- پاکسازی Outbox در فایل pfr با **پنجرهٔ زمانی ۳۰ دقیقه** (`type IN (...) AND createdAt > now-30min`) — بیش‌ازحد پهن است و رخدادهای سایر فلوها/ران‌های موازی را هم می‌بلعد (شواهد Outbox را آلوده می‌کند، نه دادهٔ تجاری را).

---

## ۵. typecheck و lint (سؤال ۵)

- **`bun run typecheck`: EXIT=2 — ۸ خطا، همه در اسکریپت‌های اسکرچ، صفر خطا در src/tests:**
  - ۳ خطای شناخته‌شدهٔ baseline: `tmp-56/cleanup-56.ts`، `tmp-57a/db-facts.ts`، `tmp-57b/cleanup-57b.ts` ✅ (همان‌هایی که مجاز بودند)
  - **+۵ خطای جدید: `tmp-60/attack1-coupon.ts`، `attack2-cancel.ts`، `attack2-setup.ts`، `attack2-verify.ts` (×۲) — «Cannot find name 'Bun'»** — مالِ ایجنت موازی 60-sec که وسط ممیزی من (12:45) اسکریپت‌های حمله را در همان tmp-60 گذاشت. من هیچ فایل .ts در ریپو نساختم (اسکریپت‌های موقت خودم را بلافاصله حذف کردم).
  - نتیجه‌گیری: شرط «فقط ۳ خطا» الان به‌دلیل همسایهٔ موازی نقض است، ولی **کد محصول و سوئیت تست صفر خطا** — و این دومین اثبات زندهٔ ضرورت QA-HYG-01 است.
- **`bun run lint`: EXIT=0 — ۰ خطا / ۹ هشدار** — دقیقاً همان ۹ هشدار قدیمی از `tmp-57a/*.js` (شواهد فاز ۱). هیچ هشدار جدیدی از فاز ۲.

## ۶. QA-HYG-01 — وضعیت exclude (سؤال ۶)

- `tsconfig.json`: `include: ["**/*.ts", "**/*.tsx", …]` + `exclude: ["node_modules", "skills", "examples", "tests", "scripts"]` — **«qa-reports» در exclude نیست** ⇒ هر .ts اسکرچی در qa-reports وارد تایپ‌کریک می‌شود (همین الان ۸ خطا: ۳ قدیمی + ۵ جدید). **پیشنهاد همچنان باز و فوری‌تر از قبل.**
- `eslint.config.mjs`: ignores شامل node_modules/.next/out/build/next-env/examples/skills — **«qa-reports» ندارد** ⇒ منبع ۹ هشدار قدیمی همین‌جاست.
- هر دو فیکس تک‌خطی‌اند و در صلاحیت فاز ۳ ثبت شده‌اند.

---

## رأی نهایی QUALITY: **B**

- **چرا نه A:** یک نقض صریح بهداشت (نشت سیستماتیک ۵ کوپن CC-% در هر ران، در برابر انتظار «صفر» — با ریشهٔ قطعی case-sensitivity در afterAll)، یک assert مرده، و ۳ شکاف نگهبانی واقعی (بازخوانی پس از قفل، مسیر usageLimit در evaluateCoupon، تعامل refundRacedPayment).
- **چرا نه C:** هر ۵ معیار پذیرش BUG-01..05 با تست بامعنا و غیرflaky (۴ اجرا، توالی یکسان بایت‌به‌بایت) پوشیده شده؛ تست‌های رقابتی همگرایی را قفل می‌کنند؛ تایپ‌کریک/لینت کد محصول و تست‌ها پاک؛ کیفیت میانگین ≈۷.۹/۱۰.
- **مسیر A (فاز ۳، همهٔاقلام کوچک):** ① فیکس یک‌خطی پسوند پاکسازی کوپن + DELETE دستی ۵۰ ردیف ② حذف assert مردهٔ pfr تست ۳ ③ سه تست شکاف (re-read قفل، evaluateCoupon-usageLimit، perUser=2 مهمان) ④ حذف deleteMany پیشوندی کاربر ⑤ پنجرهٔ ۳۰ دقیقه‌ای Outbox را به orderId محدود کن ⑥ tsconfig/eslint exclude «qa-reports».

*شواهد خام در `qa-reports/tmp-60/` (فایل‌های `int-run*`, `cases-run*`, `p2-run*`, `all-run.raw` مالِ این ممیزی‌اند؛ بقیهٔ محتوای tmp-60 متعلق به ایجنت‌های موازی است).*
